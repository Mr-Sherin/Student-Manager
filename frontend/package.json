{
  "name": "frontend-workspace",
  "private": true,
  "scripts": {
    "start": "set PORT=3001&& npm --prefix frontend start",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend test",
    "eject": "npm --prefix frontend run eject"
  }
}